# M0 桌面验收执行记录

日期：2026-08-31  
状态：已完成  
范围：V0.1 桌面验收收口，为 V0.2 桌面版启动提供基线

## 1. 自动化证据

| 检查     | 结果 | 证据                                                                                                         |
| -------- | ---- | ------------------------------------------------------------------------------------------------------------ |
| 格式     | 通过 | `npm run format:check`                                                                                       |
| 静态检查 | 通过 | `npm run lint`                                                                                               |
| 类型检查 | 通过 | `npm run typecheck`                                                                                          |
| 单元测试 | 通过 | `npm run test`：8 个文件、42/42 测试                                                                         |
| 生产构建 | 通过 | `npm run build`：132 个模块，生成 `dist/`                                                                    |
| 端到端   | 通过 | `npm run test:e2e`：Chromium、Firefox、WebKit 完整矩阵 33/33 全部通过                                        |
| 视觉回归 | 通过 | `npm run test:visual`：Chromium 单进程比较 3/3 场景、13/13 个不可变视觉基线；12 张卡片图来自真实 2× PNG 导出 |
| 依赖审计 | 通过 | `npm audit --audit-level=high`：0 vulnerabilities                                                            |

Playwright 在受限沙箱中不能启动本地浏览器（`spawn EPERM`）；在允许启动仅访问 `127.0.0.1` 的本地浏览器后，完整三浏览器矩阵通过。这是执行环境限制，不是产品缺陷。

## 2. 当前验收状态

| M0 完成条件                              | 状态             | 说明                                                                                                           |
| ---------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------- |
| 自动化质量门                             | 通过             | 见上表                                                                                                         |
| 1:1 方形样图视觉修订 (V02-VIS-00)        | 已修复并通过测试 | 分数换行、轴名截断、多内容状态下（有描述/无描述/有理由/无理由）空间平衡已完成并自动化验证                      |
| 卡片理由与内容自适应 (V02-LAYOUT-00..02) | 已完成并通过测试 | 全比例、有/无描述、有/无分项理由自适应编排与图表尺寸缩放已完成，11 比例 × 2 主题 DOM 溢出测试全部通过          |
| 五角星与香蕉皮交互控件                   | 已完成并通过测试 | 五角星为纵向紧凑 32×40px、桌面十星无滚动、未评分不作数、最低一星且无归零；香蕉皮为 44×44px 并保留归零          |
| 稀疏理由与合并分项统一契约               | 已完成并通过测试 | 严格遵循“所有启用分项均保留、理由按需出现”契约，无理由分项保留名称与分数，理由分项在下方渲染理由文本           |
| 国际化 i18n 完整性                       | 已完成并通过测试 | 未评分/最低一星读屏文本、香蕉皮重置及数值标签均完成中英文覆盖并通过自动化切换验证                              |
| 横向、准横向、方形、纵向截图基线         | 已完成并归档     | 四大布局族、明暗主题及多种内容密度状态的高清视觉基线已全部生成并归档至 `doc_CN/artifacts/baselines/`           |
| 鼠标、键盘、读屏、高 DPI 冒烟            | 通过             | 用户在 Windows 10/11 x64 目标桌面范围完成测试并确认未发现问题                                                  |
| 真实封面与 11 比例 PNG 检查              | 通过             | 11 比例初验及 1:1、4:5、9:16 修复后复验通过；页脚两端分离，无重合、截断或静默缺失                              |
| 最长合法文本 PNG 检查                    | 通过             | 用户人工检查未发现重合、截断或导出异常                                                                         |
| 发布标识 ADR                             | 已确认           | `com.xdrate.music`、`XDRate.Music`、`@xdrate/music` 与安装包名 `XDRate Music` 已冻结；图标作为 PKG-01 资产交付 |
| 首发平台矩阵                             | 已确认           | 首发仅支持 Windows 10/11 x64；其他架构与平台暂不在首发支持范围                                                 |
| 签名与更新策略                           | 已确认           | 正式包强制 OV Authenticode + RFC 3161 时间戳；`0.2.0` 使用 GitHub Releases 手动更新且不包含 updater 权限       |

独立移动端的真实 iOS Safari、Android Chrome 及移动触控验收已后移至 v0.4，不属于当前桌面版本或 V0.2 桌面版的阻塞条件。v0.3.0 小红书小工具另设容器和真实设备专项门禁。

## 3. 已验证的回归范围

- 百分制综合分与 `music-linear-100-v4` 标识；
- Archive Schema v2 自动保存及刷新恢复；
- 无第三方请求与 axe 自动无障碍扫描（符合 WCAG 2.2 AA）；
- 11 种比例 × 明暗主题的逻辑画布与 DOM 溢出；
- 10 颗紧凑五角星（未评分或 1–10 整数，32×40px，桌面无滚动，无数值框/归零）与 5 个香蕉皮（-5–0，44×44px，保留 0.1 输入与归零）的键盘操作和读屏状态；
- 统一分项卡片契约（“所有启用分项均保留、理由按需出现”），在稀疏理由状态下完整保留全部启用轴的名称和分数，并展示对应理由；
- `showReasons` 开启时，分项评分与分项理由一体化呈现，雷达图独立图例完整保留；关闭时理由区消失；四大布局族契约完全统一；
- 内容画像、密度计划和最多两次确定性降级的 55 个比例/内容组合单元夹具及稀疏理由分析单元测试；
- 1:1 方形默认图例分数单行（`x.x`）不分行、轴名完整无截断；
- 内容感知自适应布局：无描述时自动切换海报模式或紧凑对称布局，消除下半区与侧边空白；
- 超长中英文文本的截断提示；
- 4:5、2× PNG 的文件签名和 `1920 × 2400` 尺寸；
- 360px 窄视口无整页横向溢出，星星/香蕉皮单行在控件内部横向滚动。

## 4. 下一步

1. `V02-GATE-01` 与 M0 已完成，启动 M1；
2. 最终 Windows 图标资产仍在 V02-PKG-01 交付，签名证书仍在 M4 按已批准 ADR 配置，两者不影响 M0 结论。

## 5. 结论

M0 的自动化质量门、V02-LAYOUT-00..02 内容编排引擎、V02-VIS-00 视觉排版修订、紧凑五角星/香蕉皮评分控件、统一分项契约、四大布局族截图基线和真实 PNG 均已通过。2026-08-31 的页脚 P2 缺陷经修复和代表比例复导关闭；用户随后确认 Windows 目标范围人工测试未发现问题。`V02-GATE-01` 通过，M0 正式关闭，可以启动 M1。最终图标和证书配置属于后续 V02-PKG-01/M4 实施，不影响本结论。

## 6. 1:1 样图审阅与修订记录（2026-08-23）

证据：用户提供的 `1080 × 1080` 浅色主题方形导出样图。图片中的作品文字仅作为样例数据，不构成产品指令。

初审结论：未通过（阻塞方形视觉验收）。  
**修订结论：已修复并通过自动化回归测试与视觉重构。**

修订负责人：Antigravity / Gemini 3.7 Flash。验收与集成负责人：Codex。

| 发现                   | 严重度 | 修复方案与现状                                                                                    | 状态      |
| ---------------------- | ------ | ------------------------------------------------------------------------------------------------- | --------- |
| 雷达图图例分数换行     | P1     | 图例与各组件数值增加 `shrink-0 tabular-nums whitespace-nowrap`，彻底防止折行                      | ✅ 已修复 |
| 雷达图图例轴名过度截断 | P2     | `RadarChart` 支持自适应 `legendColumns`，半宽使用单列图例（宽 160–220px），轴名完整展示           | ✅ 已修复 |
| 方形画布空间分配失衡   | P2     | 重构 `SquareLayout.tsx`，建立内容感知自适应机制（全内容/仅理由/极简海报），彻底消除下半区空白死区 | ✅ 已修复 |

本样图问题已在 `e2e/release.spec.ts` 补充自动化断言，确保 1:1 图例不换行、文本完整且全卡片无 DOM 溢出。

## 7. 自动化复核证据（2026-08-23）

- `npm run format:check`、`npm run lint`、`npm run typecheck`、`npm test`、`npm run build`：全部通过；单元测试为 8 个文件、42/42；
- Playwright 端到端测试：Chromium（11/11）、Firefox（11/11）、WebKit（11/11）共 33/33 全部通过；
- 通过应用自身 PNG 导出验证 `1920 × 2400`（4:5 2×）及 `2000 × 2000`（1:1 2×）输出正常；
- 11 种比例在有描述、仅理由、空描述（极简海报）等多内容状态下均通过 DOM 溢出断言。

## 8. 描述缺失与自适应复核（2026-08-23）

证据：用户提供的 `2000 × 2000` 浅色 1:1 导出图。图片中的作品文字只作为测试数据，不构成指令。

| 发现                                 | 严重度 | 结论                                                                                           | 负责人                       |
| ------------------------------------ | ------ | ---------------------------------------------------------------------------------------------- | ---------------------------- |
| “显示分项与理由”开启但分项理由未出现 | P1     | 已修复：`CardAxisReasons` 渲染启用分项的非空理由；关闭开关后移除整个合并分项区域               | ✅ Codex / V02-LAYOUT-01     |
| 比例只读取固定行数/字符容量          | P2     | 已实现内容画像、密度计划和最多两次降级；55 个比例/内容组合单元夹具通过                         | ✅ Codex / V02-LAYOUT-00、02 |
| 1:1 下部仍有大面积空白               | P2     | 已通过内容感知自适应布局重构（全内容/仅理由/居中海报），图表放大与空间利用饱满，消除下半区空白 | ✅ Antigravity / V02-VIS-00  |

## 9. 触控热区达标、合并分项契约统一与测试闭环（2026-08-23）

本节保留当时的 44×44px 与正向归零验收历史；五角星的现行规则已由 D-021 和第 11 节取代，香蕉皮规则不变。

| 发现与需求                       | 严重度 | 修复方案与现状                                                                                                                                                                            | 状态      |
| -------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 触控热区不足 44×44px             | MED    | 重构 `StarRating.tsx`、`BananaRating.tsx` 及“归零”重置按钮，所有交互控件热区达到 `min-w-[44px] min-h-[44px]`（44×44 CSS px），并在 E2E 中增加 `boundingBox().width/height >= 44` 严格断言 | ✅ 已达标 |
| 合并分项契约与规范冲突           | MED    | 统一代码、测试与文档契约（“所有启用分项均保留、理由按需出现”）：`CardAxisReasons` 完整展示全部启用分项的名称与分数，仅在有理由的分项下方展示理由文本，稀疏理由不再造成无理由分项消失      | ✅ 已统一 |
| 四大布局族合并分项契约统一       | MED    | 统一方形、纵向、横向、准横向布局中 `CardAxisReasons` 渲染规则，彻底消除重复分项容器                                                                                                       | ✅ 已统一 |
| 稀疏理由与图标评分交互全流程测试 | MED    | `e2e/release.spec.ts` 增加 44×44px 边界检测、稀疏理由下各分项存在性检测，以及星星/香蕉皮键盘、无障碍 axe 扫描、归零及语言切换全矩阵测试（33/33 通过）                                     | ✅ 已通过 |
| 360px 窄窗口整页横向溢出         | MED    | 编辑器网格与预览容器增加明确收缩边界；星星和香蕉皮保持单行及 44×44px 热区，并在控件内部横向滚动；E2E 断言页面 `scrollWidth <= clientWidth`                                                | ✅ 已修复 |

## 10. M0-VIS: 最终视觉优化与截图基线归档（2026-08-28）

本次视觉优化针对四大布局族、字体度量、卡片垂直居中平衡及深浅主题对比度进行了全面收口，并归档 13 项确定性视觉基线。其中 12 张卡片基线来自应用真实 2× PNG 导出，另 1 张为完整评分编辑器界面基线：

| 基线文件名                                 | 布局族 / 比例       | 主题   | 内容状态                       | 验证重点                                                 |
| ------------------------------------------ | ------------------- | ------ | ------------------------------ | -------------------------------------------------------- |
| `baseline-square-1-1-light.png`            | 方形（1:1）         | 明亮   | 完整内容（带理由、评价与故事） | 左侧雷达图自适应撑满高度，右侧理由与正文层次清晰         |
| `baseline-square-1-1-dark.png`             | 方形（1:1）         | 深色   | 完整内容                       | 磨砂玻璃质感、渐变底色与高对比度文字                     |
| `baseline-square-sparse-reasons-light.png` | 方形（1:1）         | 明亮   | 仅稀疏理由（无总体评价/故事）  | 自适应切换紧凑对称双列布局，消除下半区空白               |
| `baseline-square-minimal-poster-light.png` | 方形（1:1）         | 明亮   | 极简海报模式（无理由/正文）    | 居中 Hero 雷达图与双列图例，空间利用饱满                 |
| `baseline-portrait-4-5-light.png`          | 纵向（4:5）         | 明亮   | 完整内容                       | 上部作品信息与分数/雷达并排，下部理由与正文 1fr 空间填充 |
| `baseline-portrait-4-5-dark.png`           | 纵向（4:5）         | 深色   | 完整内容                       | 深色模式纵向排版自适应与清晰对比                         |
| `baseline-portrait-9-16-light.png`         | 纵向窄版（9:16）    | 明亮   | 完整内容                       | 超窄比例自适应缩小内边距与字体，无 DOM 溢出              |
| `baseline-portrait-9-16-dark.png`          | 纵向窄版（9:16）    | 深色   | 完整内容                       | 深色窄版卡片排版稳定性                                   |
| `baseline-landscape-16-9-light.png`        | 横向（16:9）        | 明亮   | 完整内容                       | 左侧作品/分数/正文，右侧雷达/理由紧凑排版                |
| `baseline-landscape-16-9-dark.png`         | 横向（16:9）        | 深色   | 完整内容                       | 深色模式横向卡片质感                                     |
| `baseline-standard-4-3-light.png`          | 准横向/标准（4:3）  | 明亮   | 完整内容                       | 左侧信息组与右侧图表区视觉对齐与平衡                     |
| `baseline-standard-4-3-dark.png`           | 准横向/标准（4:3）  | 深色   | 完整内容                       | 深色模式准横向布局稳定性                                 |
| `baseline-rating-editor-full.png`          | 评分编辑器（Web端） | 全状态 | 专业模式 + 负面项 + 预览栏     | 紧凑星星、香蕉皮控件、计算展开与吸顶预览栏整体协调性     |

视觉比较脚本：`e2e/baselines.spec.ts`；专用配置：`playwright.visual.config.ts`；图片存储目录：`doc_CN/artifacts/baselines/`。`npm run test:visual` 仅比较且不修改基线；只有经人工确认后才能执行 `npm run baseline:update` 显式更新基线。默认 `npm run test:e2e` 排除视觉更新脚本并稳定保持三浏览器 33 项功能回归；CI 在 `windows-latest` 上使用单 Chromium 作业执行只读视觉比较，以匹配 Windows 10/11 x64 首发平台。

## 11. 紧凑星级、未评分语义与算法 v4（2026-08-28）

- D-021 首先将五角星热区从 44×44px 收紧为 36×36px；D-022 进一步调整为 32×40px 纵向长方形，并把轴名称/评分栏改为 40%/60%；香蕉皮仍为 44×44px。
- Chromium 自动化在 1280px 桌面验收视口确认十颗星完整可见且 `scrollWidth <= clientWidth`；360px 窄屏继续使用控件内滚动作为响应式回退。
- 新轴和模板应用后的正向分为 `0`（未评分哨兵），界面显示空星和“未评分，不计分”，不提供正向归零按钮；首次评分后最小为 1.0。
- `music-linear-100-v4` 从综合分分子、权重分母、评分卡分项和雷达/条形图中排除未评分轴。
- Archive Schema 仍为 v2；D-023 之前的 v3/v4 小数正分迁移规则由第 12 节取代，0 分仍转为未评分。

## 12. 整星输入与综合分精度（2026-08-28）

- D-023 取消正向数值输入框；星级点击、方向键与 PageUp/PageDown 都只产生 1–10 整数，未评分仍为 0 哨兵。
- 存档正向分校验收紧为 `0 | integer(1..10)`；导入旧小数时四舍五入到最近整星，7.4 → 7、7.5 → 8。
- 综合分公式、权重和最终一位小数均不变；整数星级配合 LV 权重仍可得到 77.5 等非整数综合分。

## 13. 可编辑专辑字段名（2026-08-28）

- D-024 新增 `WorkMetadata.albumLabel`；空值使用本地化“专辑 / Album”，自定义值最长 24 字符。
- 编辑器中的字段名可直接修改；覆盖值随自动保存和完整 JSON 存档恢复，并在卡片显示“字段名：内容”。
- 静止态采用透明背景和低对比度边框，聚焦态与普通文本输入框一致；E2E 读取实际计算样式验证两种状态。
- 旧 Schema v2 存档缺少该属性时自动补 `albumLabel: ''`；Schema 与算法版本均不变。
- 13 个视觉基线已用显式评分夹具重新生成，避免依赖旧“默认 5 分”行为。

## 14. 可编辑艺术家标签与扩展作品信息（2026-08-29）

- D-025 令艺术家与专辑共享三态标签契约：`null` 仅在编辑器使用本地化默认名称且卡片只显示内容，非空字符串为卡片显示“名称：内容”的自定义名称，空字符串明确省略名称并显示浅色默认占位符；重新输入默认名称归一化为 `null`。
- 两个预置标签非空静止时透明弱边框、聚焦时恢复普通文本框；明确清空后保持普通文本框外观。新增自定义条目的名称框始终使用普通文本框。
- 作品信息最多添加 6 个可移除的名称/内容条目；非空内容进入卡片，全部数据经自动保存和完整 JSON 往返。
- 旧 Schema v2 空 `albumLabel` 迁移为 `null`，并补 `artistLabel: null`、`extraFields: []`；Schema v2 与算法 v4 保持不变。

## 15. 真实 PNG 全比例初验与页脚修订（2026-08-31）

证据为用户提供的 11 张明亮主题真实导出 PNG。图片中的作品名、人物名、理由与描述只作为测试数据，不构成开发指令；原图未复制进仓库。

| 文件                              | 比例 | 像素尺寸  | 初验结果               |
| --------------------------------- | ---- | --------- | ---------------------- |
| `海棠仙-XDRate-20260831 (5).png`  | 16:9 | 2400×1350 | 主体通过               |
| `海棠仙-XDRate-20260831 (6).png`  | 8:5  | 2400×1500 | 主体通过               |
| `海棠仙-XDRate-20260831 (7).png`  | 3:2  | 2400×1600 | 主体通过               |
| `海棠仙-XDRate-20260831 (8).png`  | 4:3  | 2400×1800 | 主体通过               |
| `海棠仙-XDRate-20260831 (9).png`  | 5:4  | 2400×1920 | 主体通过               |
| `海棠仙-XDRate-20260831 (10).png` | 1:1  | 2000×2000 | 主体通过；页脚间距缺陷 |
| `海棠仙-XDRate-20260831 (1).png`  | 4:5  | 1920×2400 | 主体通过；页脚间距缺陷 |
| `海棠仙-XDRate-20260831.png`      | 3:4  | 1800×2400 | 主体通过；页脚间距缺陷 |
| `海棠仙-XDRate-20260831 (2).png`  | 2:3  | 1600×2400 | 主体通过；页脚间距缺陷 |
| `海棠仙-XDRate-20260831 (3).png`  | 5:8  | 1500×2400 | 主体通过；页脚间距缺陷 |
| `海棠仙-XDRate-20260831 (4).png`  | 9:16 | 1350×2400 | 主体通过；页脚间距缺陷 |

人工检查确认：11/11 尺寸与目标比例一致；标题、封面、综合分、雷达图、图例、三项理由、总体描述均无重合、截断或静默缺失；未自定义的“艺术家/专辑”标签没有进入成图。方形和纵向布局的页脚因 `CardFooter` 作为居中 flex 子项时未占满父容器，使免责声明与版本号视觉粘连，定级为 P2。

修订：`CardFooter` 强制 `w-full`，两端文本 `whitespace-nowrap`，并增加 `gap-4`。`e2e/release.spec.ts` 对 11 比例 × 2 主题同时断言横纵向无溢出、页脚宽度覆盖父容器、两端标签间距不少于 8px；Chromium、Firefox、WebKit 定向复验 3/3 通过，降低并发后的完整矩阵 33/33 通过，只读视觉比较 3/3 通过。

复验补充：用户提供修复后的 1:1（2000×2000）、4:5（1920×2400）和 9:16（1350×2400）真实 PNG。三张图片的免责声明均位于左侧、版本号均位于右侧，间距清晰；标题、封面、综合分、雷达图、图例、理由和描述继续保持无重合、截断或静默缺失。

结论：真实封面与 11 比例 PNG 项通过，页脚 P2 缺陷关闭。

## 16. Windows 人工验收结论（2026-08-31）

- 验收人：用户 / 产品负责人。
- 环境：Windows 10/11 x64 首发目标桌面范围；具体系统构建号、浏览器与读屏软件版本未单独提供。
- 范围：鼠标、键盘、读屏、高 DPI、中英文最长合法文本真实 PNG，以及前述页脚修复后代表比例。
- 结果：用户明确反馈“人工测试未发现问题”；未新增缺陷。
- 关联缺陷：第 15 节页脚 P2 已修复并通过 1:1、4:5、9:16 复验；无开放缺陷链接。
- 验收结论：通过。`V02-GATE-01` 完成，M0 正式关闭，进入 M1。
