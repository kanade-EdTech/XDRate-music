# V0.3.1 Bili Toy M0 首步执行记录

状态：BTY-M0-01 已安装验证；BTY-M0-03 已完成首个独立构建；Toy 登录和线上预览尚未开始

日期：2026-09-18

## 1. 本次完成

- 安装官方 Toy CLI `v0.5.0`，并确认 `toy upgrade` 已是最新版本。
- 读取 `toy --help-json`，确认当前 CLI 支持 `create`、`update`、`mylist`、`whoami` 和 JSON 输出。
- 新增独立 Toy 入口：`toy/index.html` 与 `src/toy/main.tsx`。
- 新增独立构建配置 `vite.toy.config.ts`，输出到 `dist-toy/`，不覆盖桌面 `dist/` 或小红书 `dist-minitool/`。
- 新增 `npm run toy:build` 与 `npm run toy:audit`。
- Toy 构建复用当前评分卡核心和浏览器文件适配器，不导入小红书 MiniTool Bridge。
- 增加 Toy 专用页面壳、离线隐私说明和能力边界提示；页面明确不联网、不读取账号、不直接发帖。

## 2. 自动化结果

| 检查 | 结果 |
| --- | --- |
| `npm run toy:build` | 通过 |
| `npm run toy:audit` | 通过，0 error / 1 warning |
| Toy 解包大小 | 428,322 bytes |
| 根入口 | 唯一 `index.html` |
| `npm run typecheck` | 通过 |
| MiniTool 单元回归 | 39/39 通过 |

审计 warning 来自评分卡导出依赖内部的资源加载器；当前构建未发现外部 URL 字符串，后续 M1/M2 将继续验证导出链路并决定是否替换该依赖。

Toy 页面壳已进入 M1-01 的工程基线，但尚未进行 Toy 平台真实预览验收。

## 5. 预览问题修复

首个线上预览出现全局样式缺失，表现为浏览器默认表单样式、评分控件错位和雷达图比例异常。原因是初版构建把 Tailwind 样式运行时注入到 JS，Toy 容器没有应用该内联样式。

已将 Toy 构建改为显式输出 `assets/style.css`，并在根 `index.html` 使用相对路径加载。修正版本地结果：3 个文件、421,063 bytes、0 error；旧预览作废，需重新生成预览后再验收。

## 3. 尚未完成与边界

- OAuth 授权尚未完成，当前 `toy whoami` 仍返回未登录；因此没有调用 `create` 或 `update`。
- 没有生成线上 `preview_url`，没有提交审核，也没有绑定视频。
- 本次只完成工程基线，不代表 Toy 已发布或审核通过。

## 4. 下一步

1. 完成 `toy login` 并记录 `toy whoami --json` 的 uid 与昵称。
2. 对 Toy 页面补齐专用标题、隐私说明和无小红书能力提示。
3. 运行 11 比例、窄屏、键盘和 PNG 预览/导出一致性回归。
4. 通过内容预检后，使用 `toy create` 仅生成预览，不加 `--yes`。
