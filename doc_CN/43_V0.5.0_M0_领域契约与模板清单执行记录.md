# V0.5.0 M0 领域契约与模板清单执行记录

状态：M0 已完成；已发现首批四个游戏模板，等待封面来源确认后进入 M1

日期：2026-09-25

## 已完成

- 新增 `src/domain/game/types.ts`，冻结游戏领域、模板来源、封面授权状态和作品字段契约。
- 新增 `src/domain/game/presets.ts`，定义 5 个稳定评价轴和共享 `music-linear-100-v4` 算法契约。
- 新增 `src/domain/game/templateInventory.ts`，登记 `模板` 文件夹中的四个游戏模板，并保留一个无封面回退夹具：红色警戒 2、使命召唤 4、原神、黑神话：悟空。
- 使用 `scripts/prepare-game-template-covers.py` 以 OpenCV 批量生成 1024×1024 WebP 处理版，保存在 `assets/game-templates/covers/`，并写入尺寸、哈希和来源清单。
- 新增 `src/domain/game/presets.test.ts`，验证轴 ID 唯一、模板槽位稳定、未确认素材不会进入公开内容。
- 中英文开发计划、路线文档和文档索引已同步。

## 当前安全门

四个游戏模板已经有作品名称、个人评分文字和对应封面文件，但目前状态为 `pending-rights-review`、封面使用状态为 `unknown`。这是刻意保留的 M0 门禁：在确认素材来源和使用范围前，不把商业封面打进公开 Toy 包；第五个槽位用于无封面回退测试。

## 验证证据

- `npm run typecheck`：通过。
- `npx vitest run src/domain/game/presets.test.ts src/domain/rating/presets.test.ts`：4/4 通过。

## M1 进入条件

1. 为四张封面记录来源、署名和 `cleared` / `local-only` / `unknown` 使用结论。
2. 确认个人评分、分项理由、扣分项和总体评价是否作为公开示例内容。
3. 确认第五个无封面回退夹具是否进入自动化测试包。
4. 通过后再实现模板套用、复制、改名、删除和用户副本隔离。
