{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window-native-files",
  "description": "XDRate desktop capability: mediated native files plus closing and titling the current app window; no arbitrary filesystem, shell, process, updater, or network access.",
  "windows": ["main"],
  "permissions": ["allow-native-files", "core:window:allow-close", "core:window:allow-set-title"]
}
