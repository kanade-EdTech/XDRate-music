# V0.5.0 M0 Domain Contract and Template Inventory Log

Status: M0 complete; four game templates found, waiting for cover-source decisions before M1

Date: 2026-09-25

## Completed

- Added `src/domain/game/types.ts` to freeze the game domain, template source, cover-usage status, and work-field contracts.
- Added `src/domain/game/presets.ts` with five stable rating axes and the shared `music-linear-100-v4` algorithm contract.
- Added `src/domain/game/templateInventory.ts` with the four game templates in `模板` plus one no-cover fallback fixture: Red Alert 2, Call of Duty 4, Genshin Impact, and Black Myth: Wukong.
- Used `scripts/prepare-game-template-covers.py` with OpenCV to generate 1024×1024 WebP derivatives under `assets/game-templates/covers/`, with dimensions, hashes, and source provenance in the manifest.
- Added `src/domain/game/presets.test.ts` to verify unique axis IDs, stable template slots, and that unapproved assets cannot enter public content.
- Synchronized the bilingual development plan, roadmap, and documentation indexes.

## Current safety gate

The four templates now have titles, personal-review text, and cover files, but remain `pending-rights-review` with cover usage `unknown`. This is an M0 gate: no commercial cover enters the public Toy package before its source and usage scope are confirmed. The fifth slot is reserved for a no-cover fallback test.

## Evidence

- `npm run typecheck`: passed.
- `npx vitest run src/domain/game/presets.test.ts src/domain/rating/presets.test.ts`: 4/4 passed.

## M1 entry conditions

1. Record provenance, attribution, and a `cleared` / `local-only` / `unknown` usage decision for the four covers.
2. Confirm which personal scores, reasons, deductions, and overall reviews may ship as examples.
3. Decide whether the no-cover fallback fixture belongs in the automated pilot package.
4. Then implement apply, copy, rename, delete, and user-copy isolation.
